# 显存占用分析：Qwen3.6 系列模型

## 模型基本信息


| 模型                | 参数量       | 类型  | 精度      |
| ------------------- | ------------ | ----- | --------- |
| Qwen3.6-35B-A3B     | 36B (激活3B) | MoE   | BF16/FP16 |
| Qwen3.6-35B-A3B-FP8 | 36B (激活3B) | MoE   | FP8       |
| Qwen3.6-27B         | 28B          | Dense | BF16/FP16 |
| Qwen3.6-27B-FP8     | 28B          | Dense | FP8       |

> **注意**：Qwen3.6-35B-A3B 是 MoE（混合专家）架构，总参数36B但每次推理仅激活约3B参数。

---

## 显存占用估算

### 估算公式基础

- **模型权重**：参数量 × 每参数字节数（FP16=2B, FP8=1B, FP32=4B）
- **优化器状态**（Adam）：参数量 × 8字节（FP32 momentum + variance）
- **梯度**：参数量 × 2字节（FP16）或 × 4字节（FP32）
- **激活值**：取决于 batch size、序列长度，变化较大

---

## 一、预训练阶段（Full Training, Adam, BF16混合精度）

预训练需要存储：模型权重(FP16) + 梯度(FP16) + 优化器状态(FP32 × 2) + 激活值


| 模型                | 可训练参数 | 权重  | 梯度  | 优化器 | 激活值(估) | **总计(估)**   |
| ------------------- | ---------- | ----- | ----- | ------ | ---------- | -------------- |
| Qwen3.6-35B-A3B     | 36B        | ~72GB | ~72GB | ~288GB | ~50-100GB  | **~480-530GB** |
| Qwen3.6-35B-A3B-FP8 | 36B        | ~36GB | ~72GB | ~288GB | ~50-100GB  | **~450-500GB** |
| Qwen3.6-27B         | 28B        | ~56GB | ~56GB | ~224GB | ~40-80GB   | **~376-416GB** |
| Qwen3.6-27B-FP8     | 28B        | ~28GB | ~56GB | ~224GB | ~40-80GB   | **~348-388GB** |

> ⚠️ 预训练通常需要多节点多卡（如 8×A100 80GB 或 8×H100），使用 ZeRO-3/FSDP 分片。

---

## 二、SFT（全参数微调，Adam, BF16混合精度）

与预训练类似，但序列长度和 batch size 通常较小，激活值开销降低：


| 模型                | 权重  | 梯度  | 优化器 | 激活值(估) | **总计(估)**   |
| ------------------- | ----- | ----- | ------ | ---------- | -------------- |
| Qwen3.6-35B-A3B     | ~72GB | ~72GB | ~288GB | ~20-40GB   | **~452-472GB** |
| Qwen3.6-35B-A3B-FP8 | ~36GB | ~72GB | ~288GB | ~20-40GB   | **~416-436GB** |
| Qwen3.6-27B         | ~56GB | ~56GB | ~224GB | ~15-30GB   | **~351-366GB** |
| Qwen3.6-27B-FP8     | ~28GB | ~56GB | ~224GB | ~15-30GB   | **~323-338GB** |

> 💡 MoE模型（35B-A3B）虽然推理时只激活3B，但**SFT全参微调时所有专家参数都需要更新**，所以显存与总参数量36B相关。

---

## 三、RL（RLHF/GRPO，以GRPO为例）

RL阶段通常需要：Policy模型 + Reference模型 + Reward模型（或规则奖励）+ 生成采样的KV Cache


| 模型                | Policy(训练态) | Reference(推理态) | 采样开销 | **总计(估)** |
| ------------------- | -------------- | ----------------- | -------- | ------------ |
| Qwen3.6-35B-A3B     | ~452GB         | ~72GB             | ~20GB    | **~544GB**   |
| Qwen3.6-35B-A3B-FP8 | ~416GB         | ~36GB             | ~15GB    | **~467GB**   |
| Qwen3.6-27B         | ~351GB         | ~56GB             | ~15GB    | **~422GB**   |
| Qwen3.6-27B-FP8     | ~323GB         | ~28GB             | ~12GB    | **~363GB**   |

> ⚠️ 如果使用外部 Reward Model，还需额外加载一个模型。实际部署通常使用 vLLM 分离推理和训练。

---

## 四、PEFT LoRA 微调（rank=64, alpha=128）

LoRA 只训练少量参数（通常为总参数的 0.5%-2%），大幅降低显存：

- 冻结权重：FP16/FP8 加载
- LoRA参数（可训练）：很少（约 0.5-1B）
- 优化器状态：仅针对 LoRA 参数


| 模型                   | 冻结权重 | LoRA参数   | 优化器 | 梯度       | 激活值   | **总计(估)**  |
| ---------------------- | -------- | ---------- | ------ | ---------- | -------- | ------------- |
| Qwen3.6-35B-A3B (BF16) | ~72GB    | ~1-2GB     | ~4-8GB | ~1-2GB     | ~10-20GB | **~88-104GB** |
| Qwen3.6-35B-A3B-FP8    | ~36GB    | ~1-2GB     | ~4-8GB | ~1-2GB     | ~10-20GB | **~52-68GB**  |
| Qwen3.6-27B (BF16)     | ~56GB    | ~0.8-1.5GB | ~3-6GB | ~0.8-1.5GB | ~8-15GB  | **~69-80GB**  |
| Qwen3.6-27B-FP8        | ~28GB    | ~0.8-1.5GB | ~3-6GB | ~0.8-1.5GB | ~8-15GB  | **~41-52GB**  |

> ✅ **LoRA 是最实用的微调方式**：
>
> - 35B-A3B-FP8 + LoRA：约 **52-68GB**，可在 1×A100 80GB 上运行
> - 27B-FP8 + LoRA：约 **41-52GB**，可在 1×A100 80GB 或 2×A6000 48GB 上运行

---

## 五、推理阶段

推理只需：模型权重 + KV Cache + 激活值


| 模型                   | 权重  | KV Cache (4K ctx) | KV Cache (32K ctx) | **总计 4K**  | **总计 32K** |
| ---------------------- | ----- | ----------------- | ------------------ | ------------ | ------------ |
| Qwen3.6-35B-A3B (BF16) | ~72GB | ~1-2GB            | ~8-16GB            | **~74-76GB** | **~80-88GB** |
| Qwen3.6-35B-A3B-FP8    | ~36GB | ~1-2GB            | ~8-16GB            | **~38-40GB** | **~44-52GB** |
| Qwen3.6-27B (BF16)     | ~56GB | ~2-3GB            | ~16-24GB           | **~58-60GB** | **~72-80GB** |
| Qwen3.6-27B-FP8        | ~28GB | ~2-3GB            | ~16-24GB           | **~30-32GB** | **~44-52GB** |

> 💡 **MoE 的推理优势**：35B-A3B 虽然总权重大，但计算时只激活3B参数，**推理速度远快于 27B Dense 模型**，吞吐量更高。

---

## 总结对比图

```
显存需求（从低到高）：

推理 < LoRA微调 <<< SFT全参微调 ≈ 预训练 < RL训练

具体推荐：
┌─────────────────────────────────────────────────────────┐
│ 场景              │ 推荐模型              │ 最低硬件     │
├─────────────────────────────────────────────────────────┤
│ 推理(短文本)      │ 35B-A3B-FP8          │ 1×A6000 48GB │
│ 推理(长文本32K)   │ 35B-A3B-FP8          │ 1×A100 80GB  │
│ LoRA微调          │ 27B-FP8              │ 1×A100 80GB  │
│ LoRA微调          │ 35B-A3B-FP8          │ 1×A100 80GB  │
│ SFT全参微调       │ 27B                  │ 8×A100 80GB  │
│ 预训练/RL         │ 任意                  │ 多节点集群    │
└─────────────────────────────────────────────────────────┘
```

---

## 关键结论

1. **推理最省显存的是 35B-A3B-FP8**（~36GB权重），且因为 MoE 只激活3B参数，推理速度极快
2. **LoRA微调性价比最高**：FP8模型 + LoRA 可在单卡 80GB 上完成
3. **27B Dense vs 35B MoE**：
   - 27B 推理时计算量更大（28B全激活），但权重更小
   - 35B-A3B 权重更大但推理计算量小（仅3B激活），速度更快
4. **全参训练（SFT/预训练/RL）** 都需要多卡甚至多节点，MoE 模型因总参数大反而更耗显存
5. **FP8 版本** 在所有阶段都能节省约 40-50% 的权重存储显存
